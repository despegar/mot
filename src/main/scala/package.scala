/**
 * Main package for Mot implementation. User-facing classes reside directly in the top-level package, with utility
 * classes residing in the [[mot.util]] sub-package. The rest of the sub-packages contain implementation classes.
 */
package object mot 